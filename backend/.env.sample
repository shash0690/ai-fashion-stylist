# ===== Backend Environment =====
PORT=5000

# Set MOCK_MODE=true to run without any external keys.
MOCK_MODE=true

# If you want real integrations, set MOCK_MODE=false and fill the keys below:

# Google Vision: either set GOOGLE_APPLICATION_CREDENTIALS to a JSON key file path,
# or use application-default credentials on your machine.
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your-service-account.json

# Hugging Face
# HF_API_KEY=hf_xxx_your_api_key
# HF_MODEL_URL=https://api-inference.huggingface.co/models/nateraw/vision-transformer